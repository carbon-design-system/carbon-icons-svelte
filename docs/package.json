{
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "bun run --cwd .. build:docs-assets",
    "prebuild": "bun run --cwd .. build:docs-assets",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "carbon-components-svelte": "^0.108.0",
    "fuzzy": "^0.1.3",
    "svelte": "^5.56.2"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.1.2",
    "carbon-preprocess-svelte": "^0.11.31",
    "vite": "^8.0.14"
  }
}
